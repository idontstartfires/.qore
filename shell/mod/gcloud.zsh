
GCLOUD_DIRECTORY="/home/q/.cache/paru/clone/google-cloud-sdk/"
if [ -f "$GCLOUD_DIRECTORY/path.zsh.inc" ]; then 
    source "$GCLOUD_DIRECTORY/path.zsh.inc"; fi
if [ -f "$GCLOUD_DIRECTORY/completion.zsh.inc" ]; then 
    source "$GCLOUD_DIRECTORY/completion.zsh.inc"; fi
