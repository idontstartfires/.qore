#!/bin/sh

# Project profile — sourced by the project script, not executed standalone.

# ROOT: absolute path to the project's root directory.
ROOT=$HOME

# Standard commands (ide, root, nav) have sensible defaults.

# Pull in shared command modules (see `project -M`):
# use gar-publish

# Add project-specific commands as cmd_<name>() functions:

# cmd_serve() {
#     cd "$ROOT"
#     ...
# }
